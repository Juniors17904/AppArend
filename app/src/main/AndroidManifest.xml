<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.apparend">
    <!-- Permisos para acceder al almacenamiento -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppArend"
        tools:targetApi="31">
        <activity
            android:name=".ui.VerBaseDatosActivity"
            android:exported="false" />
        <activity
            android:name=".ui.VisorCotasActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AgregarEstructuraActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AgregarNuevoTrabajoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.HistorialActivity"
            android:exported="false" /> <!-- Pantalla de inicio (Splash) -->
        <activity
            android:name=".ui.PantallaInicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Pantalla principal -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" />
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>